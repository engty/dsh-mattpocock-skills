---
name: grill-with-docs
description: A relentless interview to sharpen a plan or design, which also creates docs (ADRs and glossary) as we go. User-invoked：type /grill-with-docs when alignment and shared vocabulary matter as much as the plan itself.
disable-model-invocation: true
user-invocable: true
---

Load both the `grilling` and `domain-modeling` skills with the skill tool and follow them together for this session.

Docs go where the repo already keeps them (CONTEXT.md, docs/adr/); create lazily, update in place - never create a second file for the same subject. When the session ends, archive the settled design tree with mnemon_document_create and record the ADR paths in mnemon_runtime_memory.
